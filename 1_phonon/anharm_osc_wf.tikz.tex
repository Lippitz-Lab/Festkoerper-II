\documentclass{standalone}
\usepackage{currfile,hyperxmp}

\input{../tikz_header.tex}
\usetikzlibrary{intersections}

\begin{document}




\begin{tikzpicture}[
declare function={ 
	la = 5;
	U(\r) = la^2 * (exp(- 2 * \r ) - 2 * exp(- \r ) );	
	E(\n) = - (la - \n - 0.5)^2;
  zv(\r) = 2 * la * exp(- \r);
  pre(\r,\n) = 1e-2 * zv(\r)^(la - \n - 0.5) * exp(-0.5 * zv(\r));
  H0(\r) = pre(\r, 0) * 1;
  H1(\r) = pre(\r, 1) * (1 - zv(\r));
	  },
]
%\useasboundingbox (0,0) rectangle (5,5);
%\draw (0,0) rectangle (5,5);

\begin{axis}[no markers, 
	samples=50,
    %      ymin=-0.3, ymax=1,
   %   xmin = -5.5, xmax = 5.5,
   % ymin = -0.03, ymax = 6,
      %  axis y line=left,
       %    axis x line=bottom,
           xtick = {0},
           xticklabels = {$R_0$},
        %   ytick = {0,0.5, ..., 5.5},
         %  yticklabels = { , $1/2$, , $3/2$, , $5/2$, , $7/2$, , $9/2$, , $11/2$},
        ytick = {0},
            xlabel = $R $,
        ylabel = Energie,    
        x label style={at={(axis description cs:1, 0)},anchor=north east, yshift=-7pt},
    y label style={at={(axis description cs: 0,1)},anchor=south east,  yshift=10pt},
           width= 5cm, height = 5cm,
  separate axis lines,
  axis x line=bottom,
  axis x line shift=5pt,
  %xlabel shift=10pt,
  axis y line=none,
  axis y line shift=10pt,
%  ylabel shift=10pt           
           ]
           
\addplot [smooth, domain=-0.7:6,  line width=1pt,
name path global=MorseCurve]    {U(x)};
 


\foreach \a in {0,...,4}
{
  		%\tikzmath{\b = E(\a); }

     
      \draw (\a,0) circle (0.2cm);

     % \draw      (-0.7, 2 )  --  (6,1) ;

      %\draw[name intersections={of=MorseCurve and HelperLine-\a}] (intersection-1) -- (intersection-2);
  



       %\addplot [smooth, fill = gray, domain=-5:5,  line width=0pt]    {E(\a) + 3 * (H\a(xi(x)) * decay(xi(x)) )^2 };
             
     %  \addplot[gray] coordinates   {(-0.7,\b )  (6,\b)} ;
     %  \addplot [smooth,domain=-5:5,  line width=0.5pt] 
      %    {E(\a) + abs(H\a(x))^2 };
%H\a(x)
}

\end{axis}


\end{tikzpicture}


\end{document}