% \documentclass{standalone}
% \usepackage{currfile,hyperxmp}

% \input{../tikz_header.tex}

% \begin{document}



\begin{tikzpicture}
%\useasboundingbox (-1.3,-1.2) rectangle (10.2,4.7);
%\draw (-1,-1) rectangle +(12,5);

\begin{groupplot}[group style={group size={1 by 2}, vertical sep={1mm} }]

    \nextgroupplot[ %xlabel={Potential $U$ (mV) }, 
    ylabel={Strom $I$  }, 
         width=55mm, height=55mm, 
        % xmin = -dE,
        %xmax = dE,
        %ymode=log, 
        %ymode=log,
         ymin = 0,
         xmin = 0,
         xmax = 5,
         %ymax = 9,
        % ymax = 15,
         %xmax=5.5, ymin = 0, ymax=7.5,
         %axis x line=center,
         %axis y line=left,
         % xmax= 2e5, unbounded coords=jump, ymin=0, ymax = 4
        % label style={font=\tiny},
        % tick label style={font=\tiny}
        %ytick= {0} , 
        xticklabels = {},
        empty line=none,
    ]


 
  
\addplot[only marks,  mark size = 1pt, ] table [ col sep=comma,
% x expr=10^\thisrowno{0} , % 
 %y expr=\thisrowno{0} , 
 x index = 0,
  y index = 1
 ] {\currfiledir data/giaever_iv.csv};

 \addplot[only marks,  mark size = 1pt, mark=o] table [ col sep=comma,
 % x expr=10^\thisrowno{0} , % 
 %y expr=10^\thisrowno{1} , 
  x index = 0,
   y index = 2
  ] {\currfiledir data/giaever_iv.csv};

  \draw (0,0) -- ({2.95609*1.1},{4.512*1.1});

  \node at (1.5,3.7){$T > T_c$};
  \node at (2.5,1){$T \ll T_c$};
  
  \nextgroupplot[ xlabel={Potential $U$ (mV) }, ylabel={ $G_{ns} / G_{nn}$ }, 
  width=55mm, height=55mm, 
 % xmin = -dE,
 %xmax = dE,
 %ymode=log, 
 %ymode=log,
  ymin = 0,
  ymax = 2.5,
  xmax = 5,
  xmin = 0,
]

\addplot[only marks,  mark size = 2pt, mark=o] table [ col sep=comma,
% x expr=10^\thisrowno{0} , % 
%y expr=10^\thisrowno{1} , 
 x index = 0,
  y index = 1
 ] {\currfiledir data/giaever_didv.csv};

 \tikzmath{\de = 1.3;}
\addplot[domain=\de:5, samples=100]{x / (sqrt(x^2 - (\de)^2))};
\draw[] (\de,0) -- (\de,5);

% \coordinate (EF2) at (mu,0);

% \coordinate (xl) at (dE,0);



    \end{groupplot}


\end{tikzpicture}


%\end{document}