% \documentclass{standalone}
% \usepackage{currfile,hyperxmp}

% \input{../tikz_header.tex}

% \begin{document}



\begin{tikzpicture}
%\useasboundingbox (-1.3,-1.2) rectangle (10.2,4.7);
%\draw (-1,-1) rectangle +(12,5);

    \begin{axis}[ xlabel={ Magnetfeld B ($\mu$T)}, ylabel={Strom   (mA)}, 
         width=110mm, height=50mm, 
        %ymode=log, 
     %   ymode=log,
       %  xmin = 0,
    %     xmax = 4.4,
       %  ymin  = 0,
        % ymax = 15,
         %xmax=5.5, ymin = 0, ymax=7.5,
        % axis x line=bottom,
         %axis y line=left,
         % xmax= 2e5, unbounded coords=jump, ymin=0, ymax = 4
        % label style={font=\tiny},
        % tick label style={font=\tiny}
       % ytick= \empty  
       legend pos= north west,
       legend style={draw=none, font=\footnotesize}
    ]


     \addplot[no marks,  mark size = 1pt] table [ col sep=comma,
     x expr=\thisrowno{0} * 0.1, % 
    y expr=(\thisrowno{1} -0.66) * 4.5, 
    % x index = 0,
    %  y index = 1
     ] {\currfiledir data/squid.csv};

 

    \end{axis}
\end{tikzpicture}

%\end{document}